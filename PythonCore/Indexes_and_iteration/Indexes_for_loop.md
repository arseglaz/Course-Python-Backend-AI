1. Почему индексация начинается с нуля?
2. Когда удобнее использовать отрицательные индексы?
3. Чем перебор `for char in text` отличается от ручного обращения `text[0]`?
4. Что значит “итерируемый объект” простыми словами?
```python

1) Это историческое решение из C. Индекс - это не "номер элемента", а смещение от начала в памяти. Первый элемент находится на 0, ноль шагов от старта, второй - на смещении 1, и так далее

2) Отрицательные индексы используем когда нужно обратиться к элементам с конца, так как мы не всегда можем знать точную длинну обьекта

3) text[0] конкретное обращение к индексу в списке
for char in text перебор всех символов по одному

4) Обьект, который можно перебрать по элементам. list(), tuple(), dict(); str(); файлы
```
