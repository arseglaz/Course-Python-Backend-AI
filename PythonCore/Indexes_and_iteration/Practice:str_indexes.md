1. Почему `city[-1]` удобнее, чем `city[len(city) - 1]`?
2. Как защититься от пустого ввода перед обращением к индексу?
3. В чем разница между символом и строкой длиной 1?
4. Почему `len()` является функцией, а не методом строки?
```python

1) city[-1] короче и читается удобнее, так же не требует знать длину строки,
city[len(city)-1] это еще лишние вычисления

2) Через if проверяем что строка не пустая
text = input("Введи город: ").strip().title()

if text:
    print(text[0])
  else:
    print("Ввод пустой")

3) В Python нет понятия символ, есть строка в данном контексте. 
Поэтому если берем символ ( индекс в списке или одну букву строки) и просто строку состоящую из одного символа, то получаем class str. Поэтому ответ на вопрос - разницы нет

4) Это осознанное решение, len() работает с любым обьектом, у которого есть длина. Если бы это был метод строки, пришлось бы писать разный синтакиси под разные типы
```
