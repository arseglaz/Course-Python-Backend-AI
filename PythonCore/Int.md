Когда использовать % в реальных задачах?
Почему / возвращает float, даже если деление ровное?
Чем count = count + 1 отличается от count += 1?
Почему bool часто появляется как результат сравнения чисел?
```python
1) для проверки четности или кратности. 
if number % 2 == 0:
  print("четное")
if number % 10 == 0:
  print("каждый десятый элемент")

2)Результат может быть дробным
num = 7
print(num / 2) 
а если нужно получить целое число при деление нужно использовать //

3) Результат одинаковый, просто count += 1 проще и быстрее писать чем count = count + 1

4) bool это тип данных у которого только два значения True False, Сравнение это вопрос "Да/Правда" или "Нет/Неправда".
Еще оказывается что bool это подтип int - получается True==1, False==0.
