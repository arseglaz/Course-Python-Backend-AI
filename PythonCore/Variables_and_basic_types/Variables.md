1. Почему в Python переменная считается ссылкой, а не контейнером?
2. Чем неизменяемость строки отличается от переприсваивания переменной?
3. Какие ошибки чаще всего возникают из-за плохих имен переменных?
4. Как понять по документации, какие методы есть у конкретного типа данных?

```python

1) Переменная в пайтон является ссылкой или ярлыком на обьект(значение) в памяти. (MAX_NUMBER = 100 - 100 это обьект в памяти, а MAX_NUMBER просто ссылается на него)
То есть можно двумя и более переменными ссылаться на один и тот же обьект 
Не знаю помоему вопрос "Почему" здесь не совсем корректен, просто так устроен язык Пайтон, в отличии от C где переменная это конкретный блок под который выделяется сколько то памяти.

2) Строки в пайтон неизменяемые обьекты, нельзя поменять символ в обьекте например. Но переменную можно переприсвоить тогда появляется новый обьект в памяти.
Старый обьект будет в памяти пока на него есть хоть одна ссылка, а потом убирается сборщиком мусора ( было бы здорово потом разобрать в деталях как он работает в пайтон, я слышал что это даже спрашивают на собесах)

3) Похожие буквы/символы l i 0 o . Слишком короткие/сокращенные имена usr_lst, obj_nm . Имена "Неправда" user_list = {} (а по сути словарь)
def calc(a, b):
    return a * b / 100  - плохо   

def calculate_discount(price, discount_percent):
    return price * discount_percent / 100  - все понятно

4) Можно так - 
dir(str)        # список всех атрибутов и методов строки
help(str.split) # подробная документация по конкретному методу

Но еще в PyCharm если зажать Ctrl и кликнуть по нужному обьекту/функции откроется файл с документацией. Такой метод как раз показывал Руслан на видео с телеграм ботом
```

