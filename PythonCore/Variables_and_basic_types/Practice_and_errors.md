1. Как читать traceback сверху вниз и снизу вверх?
2. Почему `input()` всегда возвращает строку?
3. Чем проверка `.isdigit()` отличается от реального преобразования через `int()`?
4. Какие ошибки лучше исправлять сразу, а какие пока можно отложить до темы условий?
```python

1) Читать снизу вверх, потому что тип ошибки это последняя строка, поднимаясь выше можно понять что вызвало ошибку и откуда она

2) Потому что input() просто читает ввод с клавиатуры и не знает магическим образом что ты имел ввиду, все что вводится - символы, поэтому Python возвращает ввод как str, а интерпретировать тип - это задача програмистов🥸

3) int() преобразует в целое число, а .isdigit() делает проверку и возвращает True or False. Метод так же не пропускает отрицательные числа, но int(-5) при проверке вернет True ( имеется ввиду, что "-5".isdigit() --> False, но просто -5 как int есть ) 

4) Пока я знаю, что нужно всегда проверять тип данных, добавлять методы .strip(), .capitalise(), .title(), так же определять все переменные. 
После if: Добавлять проверку ввода пользователя на .isdigit(), деление на ноль, проверка отрицательных значений
```
