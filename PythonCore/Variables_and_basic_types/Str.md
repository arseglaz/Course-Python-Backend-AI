1. Почему методы строк можно вызывать цепочкой?
2. В чем разница между `.title()` и `.capitalize()`?
3. Когда лучше использовать f-строки вместо сложения строк?
4. Какие проблемы возникают, если не очищать пользовательский ввод через `.strip()`?

```python
1) Каждый метод сроки создает новый обьект и к новому обьекту можно вызвать другой метод 

2).title() - первая буква заглавная у всех слов.
  .capitalize() - только у первого слова заглавная буква
В реальных задачах .title() для заголовков, имён 
.capitalize() для предложений

3)Опять же, упрощение для удобства написания и чтения. Когда большая строка и много переменных, то сложно писать и читать если каждой переменной прописывать тип

4) Лишние пробелы вызывают баги, банально имя пользователя с пробелом и без это два разных логина. Оказывается еще .lower() используют для инпутов
```
