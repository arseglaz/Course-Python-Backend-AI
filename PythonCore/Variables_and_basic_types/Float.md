1. Почему десятичные дроби могут храниться неточно?
2. Когда стоит использовать `Decimal` вместо `float`?
3. Чем округление результата отличается от изменения самого числа?
4. Какие ошибки возникают при смешивании строк и дробных чисел?

```python
1)Компьютер хранит числа в двоичной системе, а большинство десятичных дробей нельзя представить точно в двоичном формате, например 0.333 по сути бесконечно, поэтому float хранит приближение 0.3

2)Decimal хранит число как int, но со смещением запятой

3) round создает новый обьект, старое значение остается неизменым

4) Python не конвертирует типы данных автоматически, поэтому при выводе надо об этом позаботиться.
  price = 3500
  label = "Цена"
  
  print(label + str(price))
  #или 
  print(f"Цена: {price}"

Пример с input: 
price = input("Уточните цену: ")
discount_percent = 12.5

discount_amount = float(price) * discount_percent / 100
final_price = float(price) - discount_amount
rounded_price = round(final_price, 2)
```
```
