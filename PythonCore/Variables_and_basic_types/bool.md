1. Чем `and` отличается от `or` на реальных примерах доступа?
2. Какие значения Python считает “пустыми” при приведении к `bool`?
3. Почему логические выражения важно разбивать на понятные переменные?
4. Как `bool` связан с будущей темой `if`?

```Python
1) and - оба условия True, or - хотя бы один. and это пропуск на закрытую территорию, or вход по любому документу

2)
False, None
bool("")   #пустые строки и коллекции
bool([])   #пустые строки и коллекции 
bool(0.0)  #числовой ноль  

3) Легко читать, понимать где ошибки

4) if это проверка на True/False
```
